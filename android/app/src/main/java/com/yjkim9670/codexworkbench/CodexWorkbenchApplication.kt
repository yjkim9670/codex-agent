package com.yjkim9670.codexworkbench

/**
 * Legacy helper retained temporarily for source history.
 *
 * It is intentionally NOT registered in AndroidManifest starting with v1.1.6.
 * Startup and system-bar handling now live in MainActivity so no project-specific
 * Application code runs before the Activity recovery boundary is available.
 */
class CodexWorkbenchApplication
