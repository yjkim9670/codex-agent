# Codex Workbench API 환산 가격 분석

기준일: 2026-08-28  
가격 출처: https://developers.openai.com/api/docs/pricing

## 원화 환산 기준

환율 기준일: 2026-08-28 KST<br>
적용 환율: **$1 = ₩1,380.9504** (USD/KRW 종가 기준, 원 단위 반올림)<br>
환율 출처: https://hamariweb.com/finance/forex/usd-to-krw-rate

아래의 원화 금액은 모든 USD API 환산액에 위 환율을 곱한 비교용 추정치다. 실제 카드·은행 결제 시에는 결제 시점 환율, 환전 스프레드, 해외결제 수수료 및 부가세가 더해질 수 있다.

## 결론

Workbench의 사용량은 ChatGPT/Codex 구독 한도나 크레딧 청구액이 아니라, 기록된 토큰을 같은 모델의 OpenAI API Standard 가격으로 환산한 값이다. 따라서 화면의 `API estimate`는 실제 청구서가 아닌 비교 가능한 기준 비용이다.

월간 추정 비용은 다음 식으로 계산한다.

`(입력 토큰 - 캐시 입력 토큰) × 입력 단가 + 캐시 입력 토큰 × 캐시 단가 + 출력 토큰 × 출력 단가`

모든 단가는 1M 토큰당 USD이며, 추론 토큰은 API usage의 output 토큰에 이미 포함되므로 별도로 더하지 않는다.

## 최근 30일 실측 사용량과 API 환산

집계 시점: 2026-08-28 13:16 KST<br>
대상 기간: 2026-07-29 00:00 ~ 2026-08-28 23:59 KST (현재까지 기록된 세션 이벤트)<br>
원본: `workspace/.agent_state/queued_codex_home/sessions/`의 각 `token_count.last_token_usage` 이벤트. 세션의 `turn_context.model`로 모델을 판별했다.

| 모델 | 요청/응답 이벤트 | 입력 | 캐시 입력 | 비캐시 입력 | 출력 | 총 토큰 | Standard API 환산 | 원화 환산 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| gpt-5.6-sol | 689 | 44,526,766 | 41,878,784 | 2,647,982 | 281,586 | 44,808,352 | $32.98 | ₩45,544 |
| gpt-5.6-terra | 1,733 | 85,675,807 | 78,873,088 | 6,802,719 | 599,491 | 86,275,298 | $36.57 | ₩50,501 |
| gpt-5.6-luna | 17 | 783,687 | 704,256 | 79,431 | 4,005 | 787,692 | $0.03 | ₩41 |
| **합계** | **2,439** | **130,986,260** | **121,456,128** | **9,530,132** | **885,082** | **131,871,342** | **$69.58** | **₩96,087** |

계산식은 모델별로 `(비캐시 입력 × 입력 단가 + 캐시 입력 × 캐시 단가 + 출력 × 출력 단가) / 1,000,000`이다. 위 기간의 총 입력 중 92.7%가 캐시 입력이므로, 총 토큰 수만 보고 비용을 곱하면 비용을 크게 과대평가하게 된다. 이 표의 요청/응답 이벤트에는 Workbench로 실행한 이 분석 작업도 포함될 수 있다.

## 주간 사용량 차감 관측과 100% 토큰량 추정

Codex Plus의 주간 표시값은 API 토큰 한도가 아니다. 동일 토큰이라도 모델, 추론량, 도구 사용, 서비스 정책에 따라 차감률이 달라질 수 있다. 따라서 아래는 **주간 10,080분 창에서 실제로 관측된 퍼센트 상승**과 그 사이의 로컬 토큰을 결합한 역산이며, 고정된 "주간 N 토큰"으로 해석하면 안 된다.

| 관측 구간 (KST) | 주간 사용량 변화 | 구간 로컬 총 토큰 | 구간 API 환산 | 단순 선형 100% 환산 | 신뢰도 |
| --- | ---: | ---: | ---: | ---: | --- |
| 8/4 16:10 ~ 8/7 19:42 | 0% → 97% (+97%p) | 51.13M | $24.47 / ₩33,792 | **52.72M / $25.23 / ₩34,841** | 높음 — 거의 한 창을 관측 |
| 8/13 13:02 ~ 8/19 15:42 | 0% → 59% (+59%p) | 6.82M | $2.84 / ₩3,922 | 11.56M / $4.82 / ₩6,656 | 중간 — 모델·작업 구성 차이 큼 |
| 8/20 16:18 ~ 8/22 20:28 | 0% → 40% (+40%p) | 4.28M | $2.12 / ₩2,928 | 10.70M / $5.31 / ₩7,333 | 중간 — 창의 일부만 관측 |
| 8/26 07:47 ~ 8/27 07:22 | 0% → 9% (+9%p) | 3.44M | $2.15 / ₩2,969 | 38.25M / $23.84 / ₩32,922 | 낮음 — 짧은 표본이라 외삽 불안정 |

가장 신뢰할 수 있는 기준은 8/4~8/7의 0%→97% 관측이다. 즉 **이 사용 패턴에서는 주간 100%가 약 52.7M 로컬 기록 토큰, API Standard 단가로 약 $25.23(약 ₩34,841)**에 해당했다. 다만 다른 주차의 동일 계산 결과가 10.7M~38.3M 토큰(또는 $4.82~$23.84 / 약 ₩6,656~₩32,922)으로 분산된다. 이는 주간 퍼센트가 단순 토큰 카운터가 아니라는 직접적인 증거다. 그러므로 운영상 기준은 `약 50M 토큰 / 약 $25 API 환산 / 약 ₩35,000`을 상한에 가까운 대표값으로 보되, 예산·한도 예측에 이를 보장값으로 사용하지 않는 것이 안전하다.

현재 관측된 새 주간 창은 8/28 13:15 KST에 12%였다. 시작 시점의 완전한 0% 표본이 없고, 이 리포트를 만드는 작업도 포함되므로 아직 100% 환산값으로 사용하지 않았다.

## 현재 Standard API 가격 (short context)

| 모델 | 입력 | 캐시 입력 | 출력 |
| --- | ---: | ---: | ---: |
| gpt-5.6-sol | $4.00 | $0.40 | $20.00 |
| gpt-5.6-terra | $2.00 | $0.20 | $12.00 |
| gpt-5.6-luna | $0.20 | $0.02 | $1.20 |
| gpt-5.5 | $5.00 | $0.50 | $30.00 |
| gpt-5.4 | $2.50 | $0.25 | $15.00 |
| gpt-5.4-mini | $0.75 | $0.075 | $4.50 |
| gpt-5.4-nano | $0.20 | $0.02 | $1.25 |
| gpt-5.3-codex | $1.75 | $0.175 | $14.00 |
| gpt-5.2 | $1.75 | $0.175 | $14.00 |
| gpt-5.1 / gpt-5 | $1.25 | $0.125 | $10.00 |
| gpt-5-mini | $0.25 | $0.025 | $2.00 |
| gpt-5-nano | $0.05 | $0.005 | $0.40 |

## 비용을 좌우하는 요소

출력 토큰은 대체로 입력보다 훨씬 비싸므로, 모델별 총 토큰보다 입력·캐시 입력·출력 분해를 함께 보는 것이 중요하다. 캐시 입력은 일반 입력 대비 약 90% 낮은 단가여서 장기 세션에서 캐시 비율이 높으면 환산 비용이 크게 낮아진다.

Workbench는 가격표에 정확히 대응하는 모델만 합계에 넣고, 과거/내부 별칭처럼 공개 가격표에 매핑할 수 없는 기록은 `unclassified_legacy`로 분리한다. 이는 비용을 임의로 추정해 과대·과소 표시하지 않기 위한 처리다.

## 실제 API 청구와 차이가 날 수 있는 경우

- 272K 이상 장문 컨텍스트는 일부 GPT-5.x 모델에서 더 높은 long-context 단가가 적용된다.
- Fast 모드는 Standard와 다른 단가이며, 구독형 Codex의 Fast 사용은 API 청구가 아닌 크레딧 배수 체계다.
- 데이터 레지던시 대상 엔드포인트는 10% 가산될 수 있다.
- 웹 검색, File Search, Hosted Shell/Code Interpreter 등 도구 호출 비용은 토큰 단가와 별도로 발생한다.
- Batch/Flex는 Standard와 다른 할인 단가다.

따라서 월간 비교에는 Usage History의 30일 모델별 `API estimate`를 사용하되, API 회계·예산 보고에는 OpenAI Usage dashboard의 실제 비용을 최종 기준으로 사용한다.
